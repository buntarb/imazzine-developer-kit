/**
 * @fileoverview Provide [{(NAMESPACE)}] base object.
 * @license [{(LICENSE_TYPE)}]
 * @author [{(EMAIL)}] ([{(AUTHOR)}])
 */

goog.provide( '[{(NAMESPACE)}]' );

/**
 * Base namespace for [{(NAMESPACE)}] module.
 * @const
 */
[{(NAMESPACE)}] = [{(NAMESPACE)}] || { };

/**
 * Bootstrap module method.
 */
[{(NAMESPACE)}].bootstrap = function( ){

	//
};
goog.exportSymbol( '[{(NAMESPACE)}].bootstrap', [{(NAMESPACE)}].bootstrap );